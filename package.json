{
  "name": "azurefunctioninterviewassessment",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "clean": "rimraf dist",
    "prestart": "npm run clean && npm run build",
    "start": "func start",
    "test": "jest"
  },
  "dependencies": {
    "typeorm": "^0.3.17"
  },
  "devDependencies": {
    "@azure/functions": "^3.0.0",
    "@types/jest": "^29.5.3",
    "@types/node": "^16.x",
    "jest": "^29.6.2",
    "rimraf": "^5.0.0",
    "ts-jest": "^29.1.1",
    "typescript": "^4.9.5"
  }
}
